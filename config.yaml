database:
  mysql:
    host: localhost
    port: 3306
    username: root
    password: "Www@123456"
    database: test
    charset: utf8mb4
    parseTime: true
    loc: Local
    maxIdleConns: 10
    maxOpenConns: 100
    connMaxLifetime: 3600  # seconds

server:
  port: 8080
  mode: debug  # debug, release, test